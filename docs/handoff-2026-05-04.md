# Aetherfall — Handoff (2026-05-04)

End of an intense day. This doc captures everything you need to pick up in a fresh session — on this device or your second device, with Claude or with Codex.

## How to resume

1. `cd C:\Automation\Github\Aetherfall && git pull`
2. Run `/aetherfall-start` (the project-scoped skill at `.claude/skills/aetherfall-start/`) to get a clean status report and surface anything that landed overnight.
3. Check whether the foundation agent's PR is open: `gh pr list`.

## What's live right now

- **URL:** https://kapitein420.github.io/Aetherfall/ (auto-deploys from `main`; Pages source was switched off the legacy `gh-pages` branch today)
- **Local dev:** `powershell -ExecutionPolicy Bypass -File ./tools/local-server.ps1` → http://127.0.0.1:4173/
- **Multiplayer:** click *Host* in the top-right panel, share the URL with `?room=...`, friend auto-joins. WebRTC peer-to-peer via PeerJS public broker, no backend.
- **Playable classes (5):** Rook (Defender), Lyra (Striker), Captain Virex (Space Pirates / Opportunist), Elyra Rootweaver (Bionic Nature Elves / Builder), Gorath Deepbreaker (Abyssal Orcs / Berserker). 15-card decks each, picked from a dropdown at setup.
- **Monsters (1):** The Hollow Titan, 120 HP, baseAttack 9.

## What shipped today (PRs #1–#11, all merged to main)

| # | What | Why it matters |
|---|------|----------------|
| 1 | `aetherfall-start` skill in repo | Cross-device session sync (you + Codex) |
| 2 | Removed broken `pages.yml` workflow | Stopped the red ✗ on every push |
| 3 | WebRTC multiplayer (PeerJS) | 2-player online co-op, no backend |
| 4 | UI overhaul Phase 1+2+3 | Big-character standoff, drag-to-target, intent telegraph |
| 5 | Battle log restructure + viewport fit | Phase banners, color-coded entries, no scroll required |
| 6 | 3 new factions (Virex / Elyra / Gorath) | 45 new cards from the worldbuilding bible |
| 7 | Layout polish | Log moved to top, central Resolve button, rules-flow strip |
| 8 | Setup screen v2 + themed multiplayer panel | Faction badges, glow rings, top-right glass MP panel |
| 9 | Faction-themed card frames | Per-class card aesthetic (Rook brass, Lyra leaf, Virex gunmetal+cyan, Elyra moss, Gorath bone) |
| 10 | Hand always visible | Cards no longer clipped at 100% zoom; dedicated bottom row |
| 11 | Dropdown contrast fix | Deck `<option>` was unreadable white-on-white |

## In flight — DO NOT START PARALLEL WORK ON ENGINE/CONTENT YET

A foundation agent is running in a background worktree (`agent-a7222e0b8b62039cf`) building the engine extensions everything else depends on. Scope:

- **Element system** — damage actions tagged with `physical | spell | water | toxic | overclock | net | biohack`; monster `elementResistances` map applies multiplier
- **Defense stat** — flat reduction on monster, separate from player Block
- **Player status counters** — generic `{ marked, frenzy, ... }` bag with helpers + per-round decay
- **HP-threshold phase machine** — monster declares `phases` array, threshold-triggered `onEnter` hooks
- **Multi-action monster turns** — `actionsPerTurn` field
- **Monster registry** — `src/content/monsters.js` with factory functions, monster selection in setup screen
- **Ironjaw Bruiser** — 2nd selectable monster as the validation case (Defense 5, water-vulnerable, toxic-resistant, Fixate at ≥15 threat, Crushing Bulwark phase at 50% HP, Pack Convergence at 25% HP with 2 actions/turn)

**When you come back, first action:** `gh pr list` — if the foundation PR is open, review it, smoke-test (pick Hollow Titan → fight; pick Ironjaw → fight; verify phase transitions, defense, element multipliers, fixate target lock). Merge if good.

## Queued work (in order)

1. **Party deck / blessings pre-battle picker** — drawn before each battle, pick 1 of 3 random buffs that apply for the fight. Independent of foundation, can run parallel with bosses once foundation lands.
2. **Boss 01 — Warden of Targeting** (control over threat; 3 phases: Surveillance → Override Protocol → Judgement Mode). Foundation must be in.
3. **Boss 02 — Tri-Core Nexus** (elemental shields cycle; 3 phases ending in Overload Collapse). Foundation must be in.
4. **Boss 03 — The Devouring Network** (steals/adapts/consumes; 3 phases ending in Singularity). Foundation must be in.
5. **Bespoke character art** — Noah will drop PNGs into `assets/ui/portraits/`. Once present, wire into `getChampionVisual` in `src/content/game-assets.js`. Naming convention suggested: `captain-virex.png`, `elyra-rootweaver.png`, `gorath-deepbreaker.png`.
6. **Engine bits flagged from earlier reviews** — `selfDamage` action + HP-scaling damage formula (true Gorath berserker), card-back art for new classes, fusion combinator (last-2-elements pattern matching for boss 02).

## Open design questions for Noah

These will block bosses-2 and party deck unless decided:

1. **Boss rewards (credits / relics / codex)** — full meta-progression system, or skip and just have bosses be alternate fights?
2. **Party deck timing** — does the picker run between setup screen and battle (single-fight context), or persist across fights (run-based context like Slay the Spire)?
3. **Are bosses optional alt-fights, or do they replace the Hollow Titan as the canonical encounter?** Determines whether monster selection is permanent or per-run.
4. **Fusion mechanic concrete spec** — the boss design mentions OVERCLOCK + NET = EMP Surge, etc. Need to know: is fusion triggered by playing two cards in sequence within a round? By having both elements in queue? By a dedicated "fuse" action? This affects how the combinator is built.

## Reference — useful paths

- Engine: `src/engine/game.js`
- Cards: `src/content/cards.js`, `src/content/classes.js`
- Monster registry (incoming): `src/content/monsters.js`
- App / render: `src/app.js`
- Styles: `src/styles.css`
- Multiplayer: `src/multiplayer/`
- Drag system: `src/ui/drag-system.js`
- Worktrees: `.claude/worktrees/agent-*` (background agent scratch spaces — don't commit these)
- Memory (only on this device): `~/.claude/projects/C--Automation-Github-Aetherfall/memory/` — user/feedback/project notes for Claude across sessions
- Design specs: in memory at `project_design_expansion.md` and `project_worldbuilding.md`

## Useful commands

```bash
gh pr list                   # see open PRs
gh pr view N                  # inspect PR details
gh pr merge N --squash        # squash-merge (only path that works — direct main push and force push are blocked)
gh api -X POST repos/Kapitein420/Aetherfall/pages/builds   # nudge a Pages rebuild if stale
```

## Constraints to remember

- **No npm, no build system.** Plain ES modules. The site IS the source.
- **Direct push to main is blocked.** Always create a feature branch + PR + squash merge.
- **Force push is blocked.** If a branch has stale remote tracking after a squash merge, branch off `origin/main` to a new name rather than try to overwrite.
- **Cards / engine API surface is stable.** `queueCard`, `unqueueCard`, `resolveRound`, `getRemainingEnergy`, `targetKey` keep their signatures. The foundation agent is briefed to preserve this.

## Health check

Site responding 200, multiplayer working, 5 classes, faction-themed cards, fits in viewport, log readable. Today's session moved the project from "single-device click prototype" to "playable online co-op deckbuilder with worldbuilding-aligned content and themed UI". Foundation work in progress will unlock the boss campaign vision next.

Sleep well.
